pipeline {
    agent any
    stages{
        stage('Build'){
            steps {
		echo 'Within build step'
                sh'mvn clean package'
            }
            post {
                success {
                    echo 'Now Archiving...'
                    archiveArtifacts artifacts: '**/target/*.war'
                }
            }
        }
    }

}