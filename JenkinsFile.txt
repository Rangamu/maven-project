pipeline {
    agent any
    stages{
        stage('Build'){
            steps {
                sh 'mvn clean PipelinePackage'
            }
            post {
                success {
                    echo 'Now Archiving...'
                    archiveArtifacts artifacts: '**/*.war'
                }
            }
        }
    }

}