pipeline {
    agent any
    stages{
        stage('Build'){
            steps {
		echo 'Within build step'
                sh 'mvn clean PipelinePackage'
            }
            post {
                success {
                    echo 'Now Archiving...'
                    archiveArtifacts artifacts: '**/*.war'
                }
            }
        }
    }

}